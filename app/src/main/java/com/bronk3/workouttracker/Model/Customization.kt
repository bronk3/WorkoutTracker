package com.bronk3.workouttracker.Model

class Customization(val id: Int, val reps: Int, val sets: Int, val measurement: Int,
                    val measurementType: String, val ExersizeId: Int, val WorkoutId: Int,
                    val timeStamp: String)